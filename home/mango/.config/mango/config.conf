# Minimal portable baseline. The complete session is implemented incrementally.
blur=0
blur_optimized=1

# Machine-specific settings belong in this unversioned optional file.
source-optional=./config.local.conf
