#!/usr/bin/env bash

set -Eeuo pipefail
# shellcheck source=/dev/null
source "$HOME/.local/lib/mangowm/common"

if mangowm_is_media_active; then
  exit 0
fi

if command -v wlopm >/dev/null 2>&1; then
  wlopm --off "*"
fi
